info:
  name: Publish
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/pull/payments/?limit=100"
  params:
    - name: from
      value: "11"
      type: query
      disabled: true
    - name: limit
      value: "100"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​
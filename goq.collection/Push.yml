info:
  name: Push
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/push/notifications/"
  body:
    type: json
    data: |-
      {
        "payload": {
          "id": "test",
          "customer_id": "kv_123",
          "amount": "56",
          "currency": "INR"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
